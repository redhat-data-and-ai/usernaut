version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - dupl
    - errcheck
    - copyloopvar
    - ginkgolinter
    - goconst
    - gocyclo
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
  settings:
    goconst:
      min-occurrences: 6
      ignore-string-values: '^[a-zA-Z_]+$'
    revive:
      rules:
        - name: comment-spacings
    staticcheck:
      checks:
        - "all"
        - "-QF*"
        - "-ST*"
  exclusions:
    rules:
      - path: "^api/"
        linters:
          - lll
      - path: "^internal/"
        linters:
          - dupl
          - lll
      - path: "_test\\.go"
        linters:
          - dupl
          - goconst

formatters:
  enable:
    - gofmt
    - goimports
