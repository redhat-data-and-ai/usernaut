#!/bin/bash
# Pre-commit checks for security and code quality

echo "🔍 Pre-commit checks..."

# Run the secrets check script
if bash scripts/check-secrets.sh; then
    echo "✅ Security check passed, proceeding with lint and tests..."
    make lint test
else
    echo "❌ Security check failed - commit blocked!"
    exit 1
fi
